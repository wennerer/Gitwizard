unit newtab;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils, Forms, Controls, Graphics, Dialogs, StdCtrls, ExtCtrls;

type

  { TNewTabForm }

  TNewTabForm = class(TForm)
    Button1: TButton;
    Button2: TButton;
    Edit1: TEdit;
    Image1: TImage;
    StaticText1: TStaticText;
    procedure Button1Click(Sender: TObject);
    procedure Button2Click(Sender: TObject);
  private

  public

  end;

var
  NewTabForm: TNewTabForm;

implementation

{$R *.lfm}

{ TNewTabForm }

procedure TNewTabForm.Button1Click(Sender: TObject);
begin
  ModalResult := mrOk;
  //close;
end;

procedure TNewTabForm.Button2Click(Sender: TObject);
begin
  ModalResult := mrCancel;
end;

end.

